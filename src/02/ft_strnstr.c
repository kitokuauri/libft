#include "libft.h"
// haystack pajar / needle aguja = encuentra la aguja en el pajar
char *ft_strnstr(const char *haystack, const char *needle, size_t len)
{
    size_t i;

    i = 0;
    if (!needle)
        return (haystack);
    {

    }
}